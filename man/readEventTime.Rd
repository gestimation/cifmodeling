% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readEventTime.R
\name{readEventTime}
\alias{readEventTime}
\title{Extract per-stratum event times from (formula, data)}
\usage{
readEventTime(
  formula,
  data,
  which_event = c("event2", "event1", "censor", "censoring", "user_specified"),
  code.event1 = 1,
  code.event2 = 2,
  code.censoring = 0,
  user_specified_code = NULL,
  subset.condition = NULL,
  na.action = stats::na.omit,
  unique_times = TRUE,
  drop_empty = TRUE
)
}
\arguments{
\item{formula}{Event(time, status) ~ strata(...)}

\item{data}{Data frame.}

\item{code.event1, code.event2, code.censoring}{Integers describing your coding (used to build d1/d2/d0 inside readSurv).}

\item{subset.condition}{Optional character expression to subset \code{data}.}

\item{na.action}{Function to handle NA (default na.omit).}

\item{unique_times, drop_empty}{See \code{cif_event_times_from_frame()}.}

\item{which}{One of "event2", "event1", "censor", "code".}

\item{event.code}{Used when which="code": status value to pick (e.g., 3 for an intercurrent event).}
}
\value{
Named list of numeric vectors (times per stratum).
}
\description{
Build per-stratum event-time lists that you can pass to \code{competing.risk.time} or
\code{intercurrent.event.time} in \code{cifplot()} / \code{cifpanel()}.
}
