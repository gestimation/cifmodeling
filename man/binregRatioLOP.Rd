% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binregRatioLOP.R
\name{binregRatioLOP}
\alias{binregRatioLOP}
\title{Fit direct binomial regression for restricted mean time lost
using a log-odds product parameterization}
\usage{
binregRatioLOP(
  formula,
  data,
  cause = 1,
  time = NULL,
  beta = NULL,
  type = c("II", "I"),
  offset = NULL,
  weights = NULL,
  cens.weights = NULL,
  cens.model = ~+1,
  se = TRUE,
  kaplan.meier = TRUE,
  cens.code = 0,
  no.opt = FALSE,
  augmentation = NULL,
  outcome = "rmtl",
  model = "log-odds",
  Ydirect = NULL,
  ...
)
}
\arguments{
\item{formula}{A model formula with an \code{Event()} response on the left-hand side.
The right-hand side defines the regression design matrix. The final column of
the resulting design matrix must correspond to a binary exposure coded as 0/1.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{cause}{Integer or vector of integers specifying the event code(s) treated
as the primary cause of interest.}

\item{time}{Numeric scalar giving the time horizon \eqn{\tau}.}

\item{beta}{Optional numeric vector of starting values. Its length must equal
the number of columns in the design matrix. By default, nuisance coefficients
are initialized at \code{0.1} and the exposure log percentage ratio parameter at
\code{log(1.2)}.}

\item{type}{Character string specifying the estimating equation. \code{"I"} gives
the basic IPCW estimator, and \code{"II"} gives the augmented estimator that
updates the estimating equation using censoring-related augmentation terms.}

\item{offset}{Optional numeric vector of offsets. Defaults to a vector of zeros.}

\item{weights}{Optional observation weights for the estimating equation.
Defaults to 1 for all observations.}

\item{cens.weights}{Optional censoring survival probabilities used for IPCW.
When supplied, internal fitting of the censoring model is skipped.}

\item{cens.model}{A right-hand side formula for the censoring model used when
\code{cens.weights} is \code{NULL}. The default \code{~ + 1} gives marginal censoring weights.}

\item{se}{Logical; if \code{TRUE}, compute robust influence-function-based standard
errors.}

\item{kaplan.meier}{Logical; if \code{TRUE}, allow Kaplan-Meier-type baseline
estimation in the censoring model fit when applicable.}

\item{cens.code}{Integer code used for censoring in the event variable.
Defaults to \code{0}.}

\item{no.opt}{Logical; if \code{TRUE}, skip numerical optimization and evaluate the
estimating equations at the supplied \code{beta}.}

\item{augmentation}{Optional numeric vector used to augment the estimating
equation. Defaults to a vector of zeros.}

\item{outcome}{Character string specifying the outcome scale. Currently only
\code{"rmtl"},  restricted mean time lost decomposition up to \code{time}, is implemented.}

\item{model}{Character string identifying the model family. Currently only
\code{"log-odds"} is implemented.}

\item{Ydirect}{Optional user-supplied outcome matrix replacing the internally
constructed IPCW outcome.}

\item{...}{Additional arguments passed through model-frame construction.}
}
\value{
An object of class \code{"binreg"} containing at least the following components:
\describe{
\item{coef}{Estimated regression coefficients. The last coefficient is the
log percentage ratio parameter for the binary exposure.}
\item{se.robust}{Robust standard errors based on the estimated influence
functions.}
\item{robvar}{Robust variance-covariance matrix.}
\item{iid}{Estimated influence functions used for robust variance estimation.}
\item{p0, p1}{Estimated conditional percentages under exposure levels 0 and 1.}
\item{p}{Estimated conditional percentage at the observed exposure level.}
\item{converged}{Logical indicating whether the numerical solver reported
convergence.}
}
}
\description{
Fits an inverse probability of censoring weighted (IPCW) ratio regression model
for the conditional proportion of outcome attributable to a specific cause in
competing risks data. The target quantity is
\deqn{E\{Z_1(\tau)\mid X\} / E\{Z(\tau)\mid X\},}
where \eqn{Z_1(\tau)} is the cause-specific restricted time lost up to the time
horizon \eqn{\tau} for cause 1 and \eqn{Z(\tau)} is the corresponding total
restricted time lost up to \eqn{\tau}.
The model is parameterized through a nuisance log-odds product model together
with a log percentage ratio parameter for a binary exposure. The final column
of the design matrix is assumed to be a binary exposure coded as 0/1, and the
remaining columns are treated as adjustment covariates.
}
\details{
Right censoring is handled through IPCW. When \code{cens.weights} is not supplied,
censoring weights are estimated internally from \code{cens.model} using
\code{\link[mets:phreg]{mets::phreg()}} and predicted censoring survival probabilities.

Let \eqn{A} denote the binary exposure in the final column of the design matrix
and let \eqn{L} denote the remaining covariates. The function models the
conditional percentage for the primary cause under a log-odds product
parameterization, returning fitted percentages under \code{A = 0} and \code{A = 1}.

The returned object includes coefficient estimates, naive and robust variance
estimates, estimated influence functions, fitted percentages, and quantities
related to the censoring model fit.
}
\examples{
## event: 0 = censoring, 1 = primary cause, 2 = competing cause
data(diabetes.complications)

fit <- binregRatioLOP(
  Event(t, epsilon) ~ fruitq1,
  data = diabetes.complications,
  time = 8,
  cause = 1,
  type = "I"
)

fit$coef
fit$se.robust

## Estimated percentages under A = 0 and A = 1 when there are no adjustment covariates
calculatePercentageLOP(
  fit$coef,
  X_L = matrix(numeric(0), nrow = 1),
  offset = 0
)

}
\seealso{
\code{\link[=Event]{Event()}}, \code{\link[=calculatePercentageLOP]{calculatePercentageLOP()}}, \code{\link[=polyreg]{polyreg()}}, \code{\link[=cifcurve]{cifcurve()}}
}
