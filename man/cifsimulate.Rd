% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cifsimulate.R
\name{cifsimulate}
\alias{cifsimulate}
\title{Simulate reusable competing-risks data}
\usage{
cifsimulate(
  n,
  effect = c("constant", "early", "late", "fh-early", "fh-late"),
  shr = 0.65,
  rho1 = 0.2,
  rho2 = 1,
  rate = c(1, 1),
  beta1.L = -0.1,
  beta2 = c(A = 0, L = 0.3),
  tau = 6,
  analysis.tau = 1,
  change.time = NULL,
  effect.rho = 1,
  effect.gamma = 1,
  grid.width = 0.01,
  prob.A = 0.5,
  prob.L = 0.5,
  censor.rate = 0,
  censor.log.hazard = c(A = 0, L = 0),
  A = NULL,
  L = NULL,
  uniforms = NULL
)
}
\arguments{
\item{n}{Positive number of subjects.}

\item{effect}{Treatment-effect shape for cause 1: \code{"constant"},
\code{"early"}, \code{"late"}, \code{"fh-early"}, or \code{"fh-late"}.}

\item{shr}{Positive subdistribution hazard ratio for \code{A = 1} when the
effect function equals one. Set \code{shr = 1} for a null scenario.}

\item{rho1, rho2}{Positive scale parameters of the baseline cumulative
subdistribution hazards.}

\item{rate}{Two positive time-scale parameters for causes 1 and 2.}

\item{beta1.L}{Log subdistribution hazard ratio for \code{L} on cause 1.}

\item{beta2}{Two log subdistribution hazard coefficients for \code{A} and
\code{L} on the raw cause-2 distribution.}

\item{tau}{Positive data-generating and administrative-censoring horizon.}

\item{analysis.tau}{Positive analysis horizon no greater than \code{tau}.}

\item{change.time}{Optional early/late change time. By default it is where
the baseline cause-1 CIF reaches half its value at \code{analysis.tau}.}

\item{effect.rho, effect.gamma}{Non-negative shape parameters for the
Fleming--Harrington-type alternatives.}

\item{grid.width}{Positive integration-grid width.}

\item{prob.A, prob.L}{Probabilities used to generate binary \code{A} and
\code{L}. \code{prob.L} also weights the marginal truth curves.}

\item{censor.rate}{Non-negative baseline exponential censoring rate.}

\item{censor.log.hazard}{Two censoring log-hazard coefficients for \code{A}
and \code{L}.}

\item{A, L}{Optional binary vectors of length \code{n}.}

\item{uniforms}{Optional named list containing any of \code{cause1},
\code{cause2}, \code{event}, and \code{censor}, each with \code{n} values in
\code{[0, 1]}.}
}
\value{
A data frame with observed \code{time}, integer \code{status} (0 censoring,
1 cause 1, 2 cause 2), factor \code{A} and \code{L}, latent event/censoring
details, and subject-specific true probabilities. Attribute \code{"truth"}
contains conditional and marginal CIF curves, the true time-varying SHR, and
time-averaged CIF contrasts. Attribute \code{"simulation_parameters"}
records the generating parameters.
}
\description{
Generates two-cause competing-risks data from a coherent restricted-CIF
construction. Cause 1 follows a possibly time-varying proportional
subdistribution-hazard model. The raw cause-2 CIF is multiplied by
\code{1 - F1(tau | A, L)}, so the terminal cause probabilities cannot sum
above one. The result can be passed directly to \code{cifcurve()},
\code{cifplot()}, \code{cifpanel()}, \code{ciftest()}, or \code{polyreg()}.
}
\details{
For \code{"early"}, the SHR equals \code{shr} before \code{change.time} and
one afterward; \code{"late"} reverses those periods. These alternatives do
not cross the null. The Fleming--Harrington-type alternatives change the SHR
smoothly using the baseline cause-1 CIF.

The final cause-2 distribution can differ slightly by \code{A} even when
\code{beta2[1] = 0}, because its raw CIF is scaled by the remaining
probability \code{1 - F1(tau | A, L)} to maintain a valid joint law.
}
\examples{
set.seed(2026)
dat <- cifsimulate(500, effect = "early", shr = 0.65)
head(dat[c("time", "status", "A", "L")])

fit <- ciftest(
  Event(time, status) ~ A,
  data = dat,
  augmentation = FALSE,
  tau = 1
)

attr(dat, "truth")$summary$time_averaged_cif_difference
}
\seealso{
\code{\link[=ciftest]{ciftest()}}, \code{\link[=cifcurve]{cifcurve()}},
\code{\link[=polyreg]{polyreg()}}
}
