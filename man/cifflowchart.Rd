% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cifflowchart.R
\name{cifflowchart}
\alias{cifflowchart}
\title{Create a flowchart of exclusions, treatment groups, and outcome status}
\usage{
cifflowchart(
  formula,
  data,
  time.point = NULL,
  pre.exclude = NULL,
  post.exclude = NULL,
  subset.condition = NULL,
  na.action = na.omit,
  outcome.type = NULL,
  code.event1 = 1,
  code.event2 = 2,
  code.censoring = 0,
  label.strata = NULL,
  order.strata = NULL,
  label.events = NULL,
  title = NULL,
  percent = TRUE,
  digits = 1,
  ...
)
}
\arguments{
\item{formula}{A formula. Supported forms are \code{response ~ arm},
\code{response ~ 1}, and \code{Event(time, status) ~ arm}.}

\item{data}{A data frame.}

\item{time.point}{Optional time point for \code{Event(time, status)} formulas.}

\item{pre.exclude, post.exclude}{Optional exclusion variables. Logical vectors
use \code{TRUE} as excluded. Character and factor vectors use non-missing,
non-empty values as exclusion reasons.}

\item{subset.condition}{Optional expression evaluated in \code{data} before
counting.}

\item{na.action}{Included for consistency with other formula interfaces.}

\item{outcome.type}{Reserved for future use.}

\item{code.event1, code.event2, code.censoring}{Status codes for event-history
formulas.}

\item{label.strata}{Optional labels for treatment groups.}

\item{order.strata}{Optional order for treatment groups.}

\item{label.events}{Optional labels for outcome states. A named vector may be
used to map existing labels to display labels.}

\item{title}{Optional graph title.}

\item{percent}{Logical; show percentages in nodes.}

\item{digits}{Number of digits for percentages.}

\item{...}{Reserved for future use.}
}
\value{
A \code{DiagrammeR::grViz()} htmlwidget.
}
\description{
\code{cifflowchart()} creates a lightweight flowchart of exclusions, treatment
groups, and outcome status with \code{DiagrammeR}. It can be used as a simple
CONSORT-like flowchart helper, but it is not intended to fully automate a
CONSORT-compliant diagram.
}
